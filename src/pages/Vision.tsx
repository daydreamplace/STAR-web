import styled from "styled-components";

const Vision = () => {
  return <Container>Vision</Container>;
};

const Container = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 100dvh;
`;

export default Vision;
